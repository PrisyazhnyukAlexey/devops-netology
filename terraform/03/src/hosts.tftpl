
[webservers]

%{~ for p in web ~}

${p["name"]} ansible_host=${p["network_interface"][0]["nat_ip_address"]}

%{~ endfor ~}

[databases]

%{~ for p in vm ~}

${p["name"]} ansible_host=${p["network_interface"][0]["nat_ip_address"]}
%{~ endfor ~}

[storage]

%{~ for p in storage ~}

${p["name"]} ansible_host=${p["network_interface"][0]["nat_ip_address"]}

%{~ endfor ~}
